---
all:
  hosts:
    armserver1:
      ansible_host: armserver1.ts.pyrah.us

    kube1:
      ansible_host: kube1.ts.pyrah.us

    kube2:
      ansible_host: kube2.ts.pyrah.us

    kube3:
      ansible_host: kube3.ts.pyrah.us

    mars:
      ansible_connection: local

rhel_servers:
  hosts:
    armserver1:

deb_servers:
  hosts:
    kube1:
    kube2:
    kube3:

desktops:
  hosts:
    mars:

ungrouped:
  hosts:
    _failwhenfound: # dummy host that triggers failure when not specifying --limit, if playbook is set up to do so
      ansible_connection: local
